<van-tabbar active="{{ active }}" bind:change="onChange"  z-index="-1">
  <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{item.normal}}" z-index="-1">
    {{
    item.text
    }}
  </van-tabbar-item>
</van-tabbar>